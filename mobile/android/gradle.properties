org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Several published plugins (audioplayers_android, rive_common) were compiled
# against compileSdk 33 while their AndroidX dependencies now require 34+. The
# AAR metadata check is disabled because the host app itself compiles against a
# newer SDK (37), so the requirement is satisfied at the app level.
android.checkAarMetadata=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
